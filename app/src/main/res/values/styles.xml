<resources>

    <style name="AppTheme" parent="BaseTheme"/>
    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>

        <item name="android:windowBackground">@color/colorWindowBackground</item>
    </style>

    <style name="DrawerMenuStyle" parent="Widget.Design.NavigationView">
        <item name="android:windowBackground">@color/colorDrawerBackground</item>
        <item name="itemBackground">@color/colorDrawerBackground</item>
        <item name="itemIconTint">@drawable/selector_drawer_content</item>
        <item name="itemTextColor">@drawable/selector_drawer_content</item>
    </style>

    <style name="ToolbarStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">?attr/colorPrimaryDark</item>
        <item name="contentInsetLeft">@dimen/contentInsetPadding</item>
        <item name="contentInsetStart">@dimen/contentInsetPadding</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    </style>

</resources>
