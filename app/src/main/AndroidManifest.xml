<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.testapp.weather"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
            android:name=".sync.SyncAdapter"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.testapp.weather.ACTION_SYNC_START"/>
            </intent-filter>
        </service>

        <provider
            android:name=".db.ForecastProvider"
            android:authorities="com.testapp.weather.forecast.provider">
        </provider>

    </application>

</manifest>
